# Video rePrompt — 项目需求文档

## 一句话简介

上传视频 → 自动识别镜头切换 → AI 反推每个镜头的视觉提示词 + 台词字幕，为 AI 视频/图片生成工作流提供素材。

## 核心功能

### 1. 视频上传
- 支持 **MP4 / MOV / AVI / MKV / WebM**，最大 2GB
- 两种上传方式：**本地文件**（拖拽/点击）和 **粘贴直链 URL**（自动下载）
- 上传进度实时显示

### 2. 镜头自动检测
- 基于 **PySceneDetect** 自动检测视频中的镜头切换点
- 支持配置 **最短镜头时长**（0.1~10秒，过滤过短的闪切）
- 检测完成后展示**缩略图胶片条**，每个镜头一张缩略图

### 3. 镜头选择
- **单击**选中单个镜头
- **Shift+单击**选中连续区间
- **Ctrl+单击**跳跃多选（非连续）
- 每个镜头可**双击预览大图**，点击播放按钮**预览视频片段**（320p + 音频）
- 选择完成后提交分析

### 4. 两种分析模式

| | 单帧推演 | 视频动态 |
|---|---|---|
| 输入 | 每镜头 1 张 JPEG | ≥2s 镜头：视频片段 / <2s 镜头：密集多帧采样（0.15s 间隔） |
| 适合 | 静态画面描述 | 动作变化、镜头运动 |
| API | `image_url` | `video_url` / 多 `image_url` |
| 输出 | 画面视觉提示词 | 动态过程视觉提示词 |

### 5. AI 视觉分析
- 支持**多模型 Provider**：阿里云 DashScope (qwen-vl-max) / OpenAI / Anthropic Claude / Google Gemini / 自定义
- 并发分析，**可配置并发数**和最大 Token
- **单帧模式**：1 张图 → 1 段描述
- **视频动态模式**：≥2s 镜头传视频片段，<2s 镜头用密集采样多帧的图片
- 提示词附带**时间感知**：极短镜头不描述成缓慢变化，正常镜头按真实时间节奏
- 输出纯中文视觉提示词，**干净可直接喂生图/生视频模型**
- 情绪仅当**面部/身体信号清晰可见**时才描述（模型自判）

### 6. 台词/语音识别（音频）
- Phase 1 提取整段 `full_audio.mp3`
- Phase 2 上传至 OSS，通过 DashScope **FileTrans 异步 API** (`qwen3-asr-flash-filetrans`) 转录
- 返回**词级时间戳**，自动按逗号分短语，按时间重叠分配到对应镜头
- 音频与画面分析**并行进行**，互不阻塞
- 识别结果**缓存复用**（不重复提交同一音频）
- 可通过配置关闭音频识别（`AUDIO_PROVIDER = none`）

### 7. 结果展示
- 每镜头卡片：缩略图、时间码、**视觉描述**、**台词文本**（如有）、视觉耗时、音频耗时
- 播放按钮可观看**视频片段回放**（含音频）
- 一键**复制单个镜头提示词**
- 一键**复制全部描述**

### 8. 导出
三种格式，浮动 FAB 按钮：
- **TXT**：纯文本，每条描述之间分隔线
- **Markdown**：带标注（时间码、镜头编号）
- **HTML**：含缩略图，可独立查看

### 9. 多次分析与翻页
- 同一视频可**多次选择不同镜头**分析
- 每次分析**独立保存**，不覆盖之前结果
- 同一视频的多轮分析**合并为一张卡片**，显示分析次数和所用模式
- 结果页面**上下翻页**切换各次分析

### 10. 后台日志
- 按日期写入 `logs/YYYY-MM-DD.log`，**毫秒级时间戳**
- 前端可查看/切换/清除日志文件

### 11. 配置管理
- **Provider / Base URL / API Key / Model / 并发数 / Max Token** 全部前端可配
- 配置通过 `localStorage` 持久化，跨会话生效
- 支持**保存多个配置预设**，一键切换
- 支持**测试连接**按钮验证 API 可用性

### 12. 附加提示词（Custom Prompt）
- 用户可追加额外指令（如"忽略水印"、"不要描述文字"等）
- 附加提示词以气泡形式**固定展示**，可删除
- 附加提示词**同步追加到视觉和音频**的分析指令中

### 13. 浏览器端持久化
- `localStorage`：最近分析列表、配置、附加提示词历史
- `sessionStorage`：当前分析进度（刷新不丢失上下文）
- 后端 `result.json`：分析结果持久化到磁盘，**服务重启不丢失**

## 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Node.js + Express |
| 文件上传 | Multer |
| 视频处理 | FFmpeg / FFprobe |
| 镜头检测 | PySceneDetect (Python CLI) |
| 视觉 AI | qwen-vl-max / GPT-4o / Claude / Gemini（OpenAI 兼容 API） |
| 音频 AI | qwen3-asr-flash-filetrans（DashScope FileTrans 异步） |
| 对象存储 | 阿里云 OSS（音频上传） |
| 前端 | 原生 JS + CSS（深色主题，无框架） |

## 关键设计原则

- **两阶段流水线**：Phase 1 检测+提取预览（让用户确认），Phase 2 分析选中镜头（按需 API 调用，节省费用）
- **0-based ←→ 1-based**：前端镜头编号从 1 开始，后端数组从 0 开始，转换点在 `/commit-range` 路由
- **缩略图复用**：Phase 1 抽取的缩略图，Phase 2 直接复用，不重复抽取
- **音频缓存**：同一视频的 `transcription.json` 按 mtime 判断有效性，不重复提交 FileTrans
- **配置覆盖链**：`localStorage overrides → .env → config.js defaults`

## 版本

当前版本：**v1.5.4**（60+ 次提交）
